{
	"name": "www",
	"version": "1.0.0",
	"description": "Project website",
	"author": "KubaP",
	"license": "MIT",
	"scripts": {
		"build": "sass styles:css && tsc --build",
		"watch-sass": "sass --watch styles:css",
		"watch-server": "live-server --watch=\"*.html\",\"css\",\"script\" --port=3000 --no-browser --no-css-inject \"./\"",
		"watch-ts": "tsc --build --watch"
	},
	"devDependencies": {
		"live-server": "^1.2.2",
		"sass": "^1.71.1",
		"typescript": "^5.3.3"
	}
}
